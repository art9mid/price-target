import { moderateScale } from 'react-native-size-matters';

export const LINE_HEIGHT = moderateScale(10);

export const DOT_BORDER_WIDTH = moderateScale(2);

export const GREY_DASHED_LINE_HEIGHT = moderateScale(37) + DOT_BORDER_WIDTH;

export const BLUE_DASHED_LINE_HEIGHT = moderateScale(20) + DOT_BORDER_WIDTH;
